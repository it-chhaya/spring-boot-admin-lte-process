version: 0.2
phases:
  pre-build:
    commands:
      - echo "Entered the pre-build phase"
  build:
    commands:
      - echo "Entered the build phase"
      - ./gradlew clean build
  post-build:
    commands:
      - echo "Enter the post-build phase"
artifacts:
  files:
    - build/libs/*
